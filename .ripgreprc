# Max 150 characters, when greater than 150 chars, show as [Omitted long line with N matches]
--max-columns=180

# Ignore *.min.js and bundled JavaScript files.
--glob=!{*.min.js,*bundle*,*vendor*,*.map}

# ====================
# Types
# --------------------
# Alias of CoffeeScript
--type-add=coffee:*.{coffee}

# Perl without Tests
--type-add=perlwt:*.{pl,pm}

# Vue sfc only
--type-add=vue:*.{vue}

